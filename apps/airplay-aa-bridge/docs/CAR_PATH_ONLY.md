# CAR PATH ONLY

**Do not** reinstall UTM, download Ubuntu ISOs, create VMs, or set up / run OpenAuto / DHU / desktop-head-unit for this project.

OpenAuto on the Linux HU was a side quest — **abandoned**. Target is **Pi → car USB AOAP only** (see `car-bringup.md`). Power still blocks (Pi undervolt when fed from host USB-C).
