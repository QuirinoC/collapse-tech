import SwiftUI
import TrustCore

/// Look confirmation is a system `confirmationDialog` in `RootView` (HIG action-sheet pattern).
/// This file is retained so the Xcode target’s source list stays stable.
enum BreakTrustPresentation {
    static let usesSystemConfirmation = true
}
