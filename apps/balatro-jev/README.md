# balatro-jev

Minimal scaffold for the Balatro Jev app.

## Setup

```bash
cp .env.example .env
npm install
npm start
```

## Scripts

- `npm start` — run the mock entrypoint
- `npm run build` — compile TypeScript (when wired)
